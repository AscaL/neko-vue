/**
 * Subpath: `neko-vue/placement` — corner / anchor → `startX` / `startY` (no Vue).
 */
export {
  cornerToStartXY,
  type NekoPlacementInput,
  type NekoStartCorner,
  resolveStartPosition,
} from "../placement/nekoPlacement.ts";
